## on dashboard page make these changes
1. on the dashboard page, bring the button or option to select and delete a workflow in front, its z-index has pushed it under the workflow cards
2. on dashboard page, add the option to doubleclick and rename a workflow 
3. on dashboard page, replace the node inspector on that page with the recent activity in that location instead

## 4. on the Workflow canvas page, make these change
4a. replace the connections component with only being able to remove the connectors by clicking on them and deleting
4b.  in module library figure LOGIC component part, integrate stuff
## 5. on the genome page 
5a. in the genome page too, make the active workflows component collapsible to the left side of the page, when it collapses just add collapse and expand chevron, add the collapse chevron near the [004] part
5b. In the genome page, when the active workflow tab collapses, let the main components like chromosome editor expand its width to take up the new space 

## 6. on evolutions page, 
6a. reduce the width of the evolution timeline component to take up less space, even the statistics component too, increase the width of generation comparison so things are not mushed 




 ## Figure also what does how to integrate openclaw and the users model etc, I need this to work mainly with openclaw and the users model etc